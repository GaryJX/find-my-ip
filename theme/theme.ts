import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: "'Nunito', 'San Francisco', 'SF Pro Text', -apple-system, system-ui, sans-serif",
    body: "'Nunito', 'San Francisco', 'SF Pro Text', -apple-system, system-ui, sans-serif",
  },
});

export default theme;
